Assume you have installed fuse aAssume you have installed fuse aOn your machine:
make
sudo mkdir /mnt
./greeter_server

On a different terminal:
sudo ./greeter_client -o allow_other /home/liuyang/Downloads/mnt
cat /home/liuyang/Downloads/mnt/hello

To unmount a mountpoint: 
sudo fusermount -u /home/liuyang/Downloads/mnt

To delete the mount folder:
sudo rm -r /mnt

//--------------------use gdb---------------------------
//------in window #1, do:
./greeter_server

//------in window #2, do:
gdb greeter_client
(gdb) run -s -d /home/liuyang/Downloads/mnt

//------in window #3, do:
ls /home/liuyang/Downloads/mnt
#other trial commands
fusermount -u /home/liuyang/Downloads/mnt

//-------------------use test.sh---------------------
//-----------test create a dir, create a file, write somthing to file, create
//----------- a tar file, remove a dir, untar a .tar file---------------------
//in a new terminal window, do:
./test.sh path/to/mount/point

//-------------------ReadWrite.cpp & run_ReadWrite.sh---------------------
//----------- test open, read, write and close a file---------------------
//in a new terminal window, do:
./run_ReadWrite.sh fileName





